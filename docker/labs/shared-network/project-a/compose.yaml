name: lab2-a

services:
  service-a:
    image: busybox:1.37.0
    command:
      - sh
      - -c
      - |
        mkdir -p /www
        echo "hello from project-a" > /www/index.html
        httpd -f -p 8080 -h /www
    networks:
      - shared

networks:
  shared:
    external: true
    name: lab_shared_net
